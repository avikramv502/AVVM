# Site Settings
title: "Aarav Vikram"
description: "Aarav Vikram — personal gateway, writing, and social links."
baseurl: "/Aarav_Vikram"
url: "https://avikramv502.github.io"

# Build Settings
plugins:
  - jekyll-feed

# Exclude from processing
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - Gemfile
  - Gemfile.lock
  - vendor/
  - _config.dev.yml
